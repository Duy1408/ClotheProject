@page
@model ClotheSystem.Pages.StaffPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Clothe[0].ClotheName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clothe[0].Rent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clothe[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clothe[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clothe[0].Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Clothe)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ClotheName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rent)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Image)
                </td>
                <td>
                    @*<a asp-page="./Edit" asp-route-id="@item.ClotheID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ClotheID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ClotheID">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
